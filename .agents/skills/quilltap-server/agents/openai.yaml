interface:
  display_name: "Quilltap Server"
  short_description: "Repo-specific patterns and workflows for quilltap-server"
  default_prompt: "Use the quilltap-server repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true